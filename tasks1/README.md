# tasks1 — JSON Task Manager Prototype

This is the first prototype for the CSC 299 project:  
a simple command-line task manager that stores tasks in a JSON file.

## How to run

From inside the `tasks1` directory:

```bash
python main.py add "Example task"
python main.py list
python main.py search Example
